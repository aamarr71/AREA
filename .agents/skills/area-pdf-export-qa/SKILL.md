---
name: area-pdf-export-qa
description: Use for AREA PDF export QA, layout checks, visual regression, print rendering, generated report quality, and browser-based PDF workflow testing.
metadata:
  short-description: QA PDF exports
---

# AREA PDF Export QA

Retrieve context first, including `context.get_area_domain_rules` with `topic: "pdf-export"`.

Validate layout, overflow, page breaks, login-gated flows, and generated artifact consistency. Record recurring bugs and layout decisions with source references.
